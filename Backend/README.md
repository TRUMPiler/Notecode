# Backend (TypeScript Express)

This folder contains a minimal TypeScript + Express setup used by Notecode.

Commands:

```
# install deps
npm install

# run in development (auto-restarts)
npm run dev

# build
npm run build

# start built app
npm start
```

The server provides placeholder `/user/login` and `/user/register` endpoints for local development.
